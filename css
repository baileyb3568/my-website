{
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  background: #111;
  color: white;
  font-family: Arial;
}

nav {
  display: flex;
  justify-content: space-between;
  align-items: center;

  padding: 20px 40px;

  background: black;
}

nav a {
  color: white;
  text-decoration: none;
  margin-left: 20px;
}

.hero {
  text-align: center;
  padding: 120px 20px;
}

.hero h1 {
  font-size: 60px;
  margin-bottom: 20px;
}

.hero p {
  max-width: 600px;
  margin: auto;
  margin-bottom: 30px;
}

button {
  padding: 15px 30px;
  border: none;
  background: cyan;
  font-size: 18px;
  cursor: pointer;
}

.cards {
  display: flex;
  justify-content: center;
  gap: 20px;

  padding: 80px 20px;

  flex-wrap: wrap;
}

.card {
  background: #222;
  padding: 30px;
  width: 250px;
  border-radius: 10px;
}
button:hover {
  background: white;
}
